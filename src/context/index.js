// we will be exporting the tracks context in here
import {
  TracksContext,
  TracksProvider,
  useTracksValue,
} from './tracks-context';

import {
  SelectedTrackContext,
  SelectedTrackProvider,
  useSelectedTrackValue,
} from './selected-track-context';

export {
  SelectedTrackContext,
  SelectedTrackProvider,
  useSelectedTrackValue,
  TracksContext,
  TracksProvider,
  useTracksValue,
};
